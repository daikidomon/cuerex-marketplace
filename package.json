{
  "name": "cuerex-marketplace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8788 --bind 127.0.0.1 --directory public",
    "validate": "python3 scripts/validate.py",
    "deploy": "npx wrangler pages deploy public --project-name=cuerex-marketplace"
  }
}
